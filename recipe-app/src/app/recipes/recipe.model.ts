export class Recipe {
	public name: string;
	public desc: string;
	public imageSrc: string;

	constructor(name: string, desc: string, imageSrc: string){

		this.name = name;
		this.desc = desc;
		this.imageSrc = imageSrc;

	}
}